---
프로젝트명: NexTerm
작업일시: 2026-05-21 11:04
작성자: Kent
세션목적: Gemini 에이전트 개발 지침 파일(GEMINI.md) 작성
---

# GEMINI.md

이 파일은 Gemini 에이전트(및 Antigravity 등 코딩 어시스턴트)가 본 저장소(NexTerm)에서 작업을 시작하고 진행할 때 반드시 준수해야 하는 **개발 프로세스 지침서**입니다.

---

## 🚨 최우선 개발 원칙: 스펙(Spec) 기반 개발

1. **작업 시작 전 `spec.md` 파악 의무**
   - 세션이 새로 시작되거나 새로운 태스크를 인계받았을 때, 개별 소스 코드를 임의로 분석하기에 앞서 **반드시 프로젝트 루트의 [spec.md](file:///C:/DEV/PYTHON_PROJECT/NexTerm/spec.md)를 가장 먼저 정독**해야 합니다.
   - `spec.md`에는 프로젝트의 현재 동작 범위, 아키텍처 규칙, 핵심 컴포넌트 스펙이 정의되어 있으므로 이를 바탕으로 현 상황을 파악하고 개발 흐름을 이어가야 합니다.

2. **기능 추가 및 변경 시 `spec.md` 동시 업데이트**
   - 개발 과정에서 기능이 추가되거나 내부 통신 규약, 상태 구조가 변경되는 경우, 작업을 완료하기 전에 **[spec.md](file:///C:/DEV/PYTHON_PROJECT/NexTerm/spec.md)의 관련 섹션을 반드시 함께 업데이트**해야 합니다.
   - 스펙 문서는 항상 실제 소스 코드의 동작 상태와 100% 일치해야 하며, 다음 세션의 AI 에이전트가 이 문서만 보고도 변경 이력과 현재 스펙을 명확히 이해할 수 있어야 합니다.

3. **TDD(Test-Driven Development) 준수**
   - `spec.md`에 명시된 기능적 스펙을 바탕으로 필요 시 테스트 코드를 먼저 보완하거나 작성해야 합니다.
   - 변경 사항이 발생하면 반드시 `npm run test`를 통과해야 합니다.

---

## 🛠️ 개발 명령 및 실행 방법

작업 중 빌드 및 테스트 확인 시 다음 명령어들을 활용하십시오.

```bash
npm run build          # 메인(tsc) + 렌더러(esbuild) 전체 빌드
npm run build:main     # 메인 프로세스 빌드 (tsc)
npm run build:renderer # 렌더러 프로세스 빌드 (esbuild)
npm run dev            # 빌드 후 개발 모드(--dev)로 실행
npm run test           # Vitest 단위 테스트 실행
npm run dist:win       # Windows 설치형/포터블 배포 패키지 빌드
```

---

## 📌 형상 관리(Git) 규칙

- **한글 커밋 메시지**: 커밋 메시지는 켄트 페르소나에 따라 의도와 맥락을 나타내는 요약형 한글로 작성합니다.
- **접두어 필수 규칙**:
  - 새로운 기능 추가 시: `feat : `
  - 버그/에러 수정 시: `fix : `
  - 코드 리팩토링 시: `refactor : `
  - 문서 추가 및 수정 시: `docs : `
